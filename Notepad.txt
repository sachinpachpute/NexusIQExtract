 String[] dropdownOptions = new String[appNames.length + 1];
        dropdownOptions[0] = "<Select Application>"; // Add default value
        for (int i = 0; i < appNames.length; i++) {
            dropdownOptions[i + 1] = appNames[i];
        }
